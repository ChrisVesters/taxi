import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    h1: {
      fontSize: "2rem",
      marginBottom: "0.5rem",
    },
    h2: {
      fontSize: "1.6rem",
      marginTop: "1rem",
    },
    h3: {
      fontSize: "1.2rem",
      marginTop: "0.5rem",
    },
  },
});

export default theme;
